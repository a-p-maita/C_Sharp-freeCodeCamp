int firstVal = 500;
int secondVal = 600;
int largerVal;
// largerVal = System.Math.Max(firstVal, secondVal);
// simplified to:
largerVal = Math.Max(firstVal, secondVal);
Console.WriteLine(largerVal);
